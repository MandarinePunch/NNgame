<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Users">
	<!-- Join -->
	<insert id ="join" parameterType="UserDTO">
		INSERT INTO USERINFO VALUES ( SEQ_USER_NO.nextval, #{user_email}, #{user_pwd}, #{user_nickname}, #{user_phone}, #{user_birth}, SYSDATE )
	</insert>
	
	<!-- Login -->
	<select id = "login" parameterType="hashmap" resultType="_int">
		SELECT COUNT(*) FROM USERINFO WHERE USER_EMAIL = #{userid} and USER_PWD = #{userpw}
	</select>
	
	<!-- UserData 조회 -->
	<select id="getUserDTO" parameterType="string" resultType="UserDTO">
        SELECT * FROM USERINFO WHERE USER_EMAIL = #{userid}
	</select>
</mapper>
